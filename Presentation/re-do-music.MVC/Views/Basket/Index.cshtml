@model ReDoMusic.Domain.Entites.Basket

<div class="container mt-3">
    <h2>Basket</h2>

    @if (Model.Items.Count == 0)
    {
        <p>Your basket is empty.</p>
    }
    else
    {
        decimal total_price = 0;
        <table class="table table-dark table-hover">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Brand</th>
                    <th>Model</th>
                    <th>Color</th>
                    <th>Price</th>
                    <th>Quantity</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Instrument.Name</td>
                        <td>@item.Instrument.Brand.Name</td>
                        <td>@item.Instrument.Model</td>
                        <td>@item.Instrument.Color</td>
                        <td>@item.Instrument.Price</td>
                        <td>@item.Quantity</td>
                    </tr>
                }
            </tbody>
        </table>
        {
            @foreach (var item in Model.Items)
            {
                total_price += item.Instrument.Price;
            }
        }
        <h3>Total Price: @total_price &#x20BA; </h3>

        <a asp-controller="Order" asp-action="AddOrder" class="btn btn-primary">Checkout</a>
    }
</div>
